# `<DOMAIN_NAME>`

A domain under `operations/`. Examples: marketing, recruitment, finance, client work.

## Shape

```
<domain-name>/
├── README.md            You are here
├── sessions/            Per-work-session writeups
├── decisions/           Decisions specific to this domain
│   └── INDEX.md
├── skills/              Domain-specific skills (optional)
├── workflows/           Domain-specific workflows (optional)
└── (your work)
```

Same session log + decision pattern as projects, just for operational work instead of code builds.
