# SFX Library · huashu-design

> All generated with ElevenLabs Sound Generation API — Apple keynote-grade audio quality.
> Production-grade SFX asset library covering all scenarios for Huashu animations, demos, and product demos.

**Asset location**: `assets/sfx/<category>/<name>.mp3`
**Total count**: 37 SFX (30 batch-generated + 7 retained from v7b)
**Generation model**: ElevenLabs Sound Generation API (prompt_influence 0.4)
**Audio quality**: 44.1kHz MP3, Apple keynote-grade clarity, no extra reverb

---

## Directory Structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick Index

### Keyboard

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | Single key press (mechanical keyboard) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | Continuous fast typing (demo: entering a prompt) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | Backspace delete | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | Spacebar tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | Enter key confirm (retained from v7b) | enter key press, crisp tactile |

### UI (Interface Interaction)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | Standard UI click (retained from v7b) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | Soft UI click (secondary buttons/links) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | Element focus/select (retained from v7b) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | Hover hint (microsecond feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | Mobile tap (iOS interface) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | Toggle switch on | ios toggle switch flip, satisfying click |

### Transition

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | Standard whoosh (retained from v7b) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | Fast whoosh (headline flash-in, tab switch) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | Horizontal swipe (carousel, tab switch) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | Element slide in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | Soft dissolve (image fade out/in) | soft dissolve, airy shimmer |

### Container (Cards / Containers)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | Card snap / lock into place (retained from v7b) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | Card flip (front/back switch) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | Stack collapse (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | Modal dialog open | modal popping open, whoosh + thud |

### Feedback (Notifications / Responses)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | Success notification (payment done, task complete) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | Error notification (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | Message pop (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | Achievement unlocked (milestone, badge) | triumphant rising arpeggio, game-style |

### Progress (Progress / Status)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | Loading pulse (progress bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | Completion confirm (step done) | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI generation starts | soft rising shimmer, magical whoosh |

### Impact (Brand / Impact)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | Logo impact (retained from v7b) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | Extended logo impact (cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | Stamp impact (certification, seal) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | Object landing (insert, place) | heavy thud, wood surface contact |

### Magic (AI Transforms)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | Magic sparkle (AI highlight, delight) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing sound (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | Transform transition (morph effect) | rising shimmer whoosh with sparkle tail |

### Terminal (Command Line)

| File | Duration | Use Case | Prompt Key |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | Command execution | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | Output appears | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | Cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended Scene Pairings

### Terminal Interaction Demo
```
type (0.5s) → enter (0.5s) → command-execute (0.5s) → output-appear (0.6s)
```
Loop element: `cursor-blink` as ambient sound during idle.

### Card Selection Flow
```
hover-subtle (0.5s, UI hover) → click-soft (0.5s, click) → card-snap (0.5s, lock in)
```
Advanced version: `card-flip` for front/back switching.

### AI Generation Full Flow
```
generate-start (0.8s, launch) → ai-process (1.2s, processing) → sparkle (0.8s, flash) → complete-done (0.8s, finish)
```
On error, replace `complete-done` with `error-tone`.

### Logo Reveal (Brand Moment)
```
whoosh-fast (0.6s, build-up) → logo-reveal-v2 (1.5s, landing) → sparkle (0.8s, tail)
```
Simple version: `whoosh → logo-reveal` (the v7b two-piece set).

### UI Interaction Demo (Mobile)
```
tap-finger (0.5s, tap) → slide-in (0.6s, panel slides in) → toggle-on (0.5s, switch)
```
After completion: `success-chime` or `notification-pop`.

### Data Visualization / Dashboard
```
loading-tick (0.5s, beat) × N → complete-done (0.8s, data arrives) → achievement (1.5s, wow moment)
```

### Form Submission Flow
```
click-soft (0.5s) → loading-tick ×2 (1.0s) → success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### Magic Transform Scene
```
whoosh-fast (0.6s) → transform (1.0s) → sparkle (0.8s)
```
Best for: element morphing, before/after effect comparison, "AI rewrite" demos.

---

## Usage Standards

### Volume Guidelines (from apple-gallery-showcase.md dual-track audio)
- **SFX main track**: `1.0` (no attenuation)
- **BGM background track**: `0.4 ~ 0.5` (SFX punches through clearly)
- **Multiple SFX stacked**: use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg Composition Template
```bash
# Single SFX aligned to a time point:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection Decision Tree
1. **Has a tactile action** (typing/clicking/swiping) → `keyboard/` or `ui/`
2. **Element entering/exiting** → `transition/`
3. **Container-level operation** (card/modal) → `container/`
4. **State feedback** (success/failure/notification) → `feedback/`
5. **Progress/time passing** → `progress/`
6. **Brand landing/important moment** → `impact/`
7. **AI magic/transform** → `magic/`
8. **Command line/code demo** → `terminal/`

### Avoid Sound Stacking
- Maximum `2 SFX` concurrent at the same timestamp
- Can go up to 3 when BGM is below 0.3
- During brand impact, clear other SFX (leave 0.2s silence before the hit)

---

## Prompt Writing Principles (for reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**Three elements of a good prompt**:
1. **Physical sound description**: what object, what action ("mechanical keyboard single key press")
2. **Texture/style qualifier**: apple-style / ios-style / cinematic / retro
3. **Exclusion of negatives**: no reverb / clean studio / minimal

No: "click sound"
Yes: "crisp ui button click, clean modern interface sound, apple-style, high pitched"

No: "magic"
Yes: "bright twinkling stars sound, high pitched glittery chime, fairy dust"

---

## See Also
- Dual-track audio and ffmpeg composition: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-time batch generator)
