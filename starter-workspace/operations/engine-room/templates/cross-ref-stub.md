# <session title> — <YYYY-MM-DD>

**Cross-reference stub.** The full session writeup lives at:

`<absolute-or-repo-relative path to the primary session log>`

This session primarily landed under `<primary domain>` but had meaningful work here (`<secondary domain>`) too. See the primary log for context.

## Scope touched in this domain
<2-4 bullets naming what was touched here specifically, so future grep of THIS folder still surfaces it.>
